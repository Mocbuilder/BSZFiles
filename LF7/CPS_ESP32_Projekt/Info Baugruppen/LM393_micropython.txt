from machine import ADC, Pin


ldr = 	?		#Pin-Deklaration
gamma = 0.7 		#Slope of the log(R) / log(lux) graph
rl10 = 50 		#LDR resistance @ 10lux (in kilo-ohms)


while True:

    #Berechnung Lux über analog.read(), Spannung und Widerstandswert
    analog_value =   ?	#Auslesung Analogwert und Division durch 4
    voltage = analog_value / 1024 * 5
    resistance = 2000 * voltage / (1 - voltage / 5)
    lux = pow(rl10 * 1e3 * pow(10, gamma) / resistance, (1 / gamma))
   
  